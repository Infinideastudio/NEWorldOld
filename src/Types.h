#ifndef TYPES_H
#define TYPES_H

#include <cstdint>
#include <glad/gl.h>

using Brightness = std::uint8_t;
using BlockID = std::uint16_t;
using TextureIndex = std::uint16_t;
using ChunkID = std::uint64_t;
using TextureID = GLuint;

#endif // TYPES_H
